The Gulp Task that generate the AboveTheFold CSS Files, put the Files here. Maybe its a Bug, but the 'index.html' have doubled Inline CSS.
Workflow take the right File and Copy the inline CSS and the Script Loader LoadCSS and paste it into your regular Files.
