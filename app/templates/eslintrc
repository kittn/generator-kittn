{
  "env": {
    "browser": true,
    "node": true,
    "es6": true,
    "jquery": true
  },
  "extends" : [<% if (projectscriptlinter == 'airbnb') { %>
    "airbnb",<% } if (projectscriptlinter == 'standard') { %>
    "standard",<% } if ( projectJSFramework === 'Vue.js' ) { %>
    "vue",<% } %>
    "plugin:import/errors",
    "plugin:import/warnings"
  ]
}
